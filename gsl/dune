(library
  (name gsl)
  (libraries
    core_kernel
  )
  (inline_tests)
  (preprocess (pps ppx_jane ppx_expect))
  (foreign_stubs (language c) (names gsl)
    (flags -I. -I/usr/include/gsl))
  (c_library_flags (-lgsl))
  (install_c_headers gsl)
)
